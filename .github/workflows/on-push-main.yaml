on: 
  workflow_call

jobs:
  publish-dev:
    strategy:
      matrix:
        environment: [dev, test, production]

    uses: ./.github/workflows/publish.yaml
    with:
      ENVIRONMENT: ${{ matrix.environment }}
  