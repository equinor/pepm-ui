import { Table } from '../components/Table'

export const Browse = () => {
  return (
    <>
      {/* models?.map((model) => (
        <p key={model.name}>{model.name}</p>
      ))*/}
      <Table />
    </>
  )
}
